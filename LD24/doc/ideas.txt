How about an Basic Artifical Intelligance.
Hmm. it could train on data the user inputs.
Maybe we should have LOTS of people testing and sending data and training.

Raycaster.. Or raytracer..

Raycasting, Casting Rays throughout the whole map and detecting walls..

3D Java Game.. Would need some basic Engine or Rendering algorithim of some sort.
BSP Trees? No thats for later.

Java Game it is...

Ok. So it would need to use Raycasting.

## Project Description

"LD24" is a Java Game that uses Raycasting.
The raycaster casts rays within the map itself and shoots it until
it exceeds RenderDistence or detects walls (If it does detect one).
It logs our X Position and RayLength and renders walls accordingly.

## MVP

A Basic Raycaster with wall colors. Including Movement such as rotation.

## Project BreakDown

Tasks:

- Raycasting
- Rendering (Done)
- Camera (Half-Finished)

Would Break it down but its all in my head :DD

Problems.

- Need To Render Wall in the middle of the screen.
 + The exact X Coordinate of the wall.
 
 Ok so.. Our 3D Game Engine didn't work out so well. What about a Text adventure game. No thats boring.
 
AI? I guess AI is our best bet Actually nevermind I am being too ambitious. Back to our 3D Engine and the drawing board.

Zombie Game for now?

## Tasks:

- Raycasting
- Rendering
- Movement

Tasks for Raycasting:

Rendering.

- Render ceilings and floors

Tasks:

- Create "Depth" inbetweeen.
- Put a perspective effect on it.


Let us try and cast a Ray by checking the X and Y Position are in the 0 (No Walls)
Then casting A Ray which extends from the player direction.

Wolfenstein styled Game Engine.


Here's how Rays will Cast.
It will cast a Ray from player angle and direction.
It will increase the ray until it detects a wall.

Shit we need to cast Rays...

First we need to note the player position on the X and Y Axis.
Then we need an angle of course.
From the angle + The player position + FOV of it we need to cast a Ray.
We then need to increase RayLength until it detects walls.

So what do we need to do know.. Umm so we have a somewhat working raycaster.
Camera bugs are finally fucking fixed (I think).. So now what? Wall Distence isn't working/
But at the sametime The Wall Rendering itself could be fucked and the raycaster too.

So lets work on the raycaster what are its bugs:
Rays aren't casted properly. The issue is we cast rays be increasing
Player Boundrys but the problme is that it can exceed map width and height.

Player position should stay and the same and Rays should be casted throughout the
FOV and Angle the player faces.

Ok but how should we do all this...
We can but should should we do all this.

Firstly we need a proper angle. It can't be 1, 2, 4, etc.
Because it represents sort of a more up and down type of thing.
So we need some math behind that.

If we take a look at our current way of casting rays our FOV and angles
are sort of a more a STRAIGHT way to do it. (Up and down) making the wall rendering
Potentionally unaccurate and +

Ok so how can we approach this. Maybe make the FOV Within the map but to the map.
So if the map size is 7 by 7 make the camera FOV FOV but that would servely limit the
walls being rendered so that wouldn't be good. So FOV cannot be part of the map.

So how can we cast a Ray from the player without having to cross boundries and essentially
exceed the FOV.

So uhh how can we do this..

Maybe we can check for each time rayxDir is increasing and make sure that its somewhat
within our FOV and at the sametime within map boundries.

Ok so we have a bug on wall rendering.

When it detects one wall it seems to render it across the entire screen. It may be that or it could
be that different walls are being incremented only a little so it looks like its stretched
across a screen.

Oh that is fixed now. Lets render multiple walls at the sametime which I have yet to do.
So why is that? Is it that the wall positions that it detects are the same.

Nevermind raycasting isn't working out. Lets try a projection thing. Lets have a vertex
representing shapes.

Then we can have a function to draw the vertexes to 2D and essentially use the Z axis
to determine the size of the vertex and basically create a size perspective effect.
We can work on lines later.

Tasks for today:

- Store vertexes in an array.
- Code a Projection system to convert vertexes to 2D.
- Render each vertex.

So how is projection gonna work..
We need a projection because when we subtract vertex by Z axis it creates a perspective
effect. It isn't true 3D oh and what about a camera system. A projection system would
move and rotate the vertexes accordingly.

How will the projection system work?

This algorithim will convert the 3D to 2D.
We can use a Raycaster to essentially detect all vertex positions on
the screen. Then increase Ray to the point on the Z axis. This is by having a Z buffer.
and creating a perspective effect to make it look close to the camera.

Or we can do a doom style renderer:

1. have a sectors and wall sizes in a file.
2. Read the sectors and wall sizes
3. Store the walls in a list of sectors.
4. Find camera position and see which wall is closest.
5. Render the wall using vertical scanlines.
6.

Cool so how can the wall rendering work?
We need camera position x y. (Angles will be worked on later)
We can use the X and Y and detect to see if the X and Y position
is number 1, in sector Z and number 2 a wall is in the camera viewpoint.
We also need to detect how far away camera is from wall.

We can use sectors to detect what walls are part of. So we can have a XDir and yDir
for camera angle. Then we number 1 detect which sector a player is in then see if the
wall is within their field of view Lets code rotation.

Lets code Angle first. We can do the Xpos + yPos and FOV to detect Wall position.
So lets code Rotation and find some clever way to do this.


How can we render walls?

First we need to know the X and Y Position of the wall.
We need to know if the camera is facing to a wall. Then
we need to Render the wall according to size.

So how can we figure out how to detect if Camera is facing wall.
Angles are the way. But what I'm not about to use Trigonometry.
So what is a clever way to rotate without trig.

Level format::

## Used to position walls

## First Point.
- X (X Position of Wall)
- Y (Y Position of Wall)

## Second Positions of the walls.

## Second Points.
- X1 (X Starting Point of Wall)
- Y1 (Y Starting Point of wall)

Maybe f = mx something..

Ok so how can we Render stuff?
We need FOV Defined first. But FOV may need to have an angle
AND it needs player position to the FOV. So at the Player X and Y
+ Fov we can find if X and Y wall is in here.

Finally a wall is rendered. We will have to fix Ceiling levels though.
Floor is fine though.

Stuck on Wall Detection.

So we have a FOV Defined great.
Now what? we need to detect walls. We need to take in the count of FOV>
Player position on the map including Wall position on them map.

We need to see if the Wall position is in the FOV at player angle + player Position.

Wall needs to be detected at x AND y position.

We need to figure out where the wall is from the camera and render it like that.

To DO:

1. Get the Full Wall Size.
2. Detect to see if the Wall Size and X and Y exceeds your FOV.
3. If so Render the Wall According to the Wall Size.

Now we detect if the Wall is within our angle.

// Get our proper wall size according to X1

Let's Work on movement now finally thank god.

Lets start with moving up and moving down.
So do we want to move the World or do we want to move the player?
walls are get according to angle. But we can move walls instead.

Lets start with moving Right. Let's Move the walls to the left if we want to move right.
// Our Line draws start from xOffset and yOffset again.. Ok its not.

Clear the screen properly it seems.


Error pinpoint.

Problem: Make the Walls move according to Move Speed.

Part of problem: When we convert double's to Int's we get dumb results.

Them doubles aint casting to ints :/

For rotation, Sprite change and increase X axis.

We can code trigonometry from scratch essentially by having forming triangles on the
X and Y axis using vectors. Then having a point on the vector. Increasing that Point
representing as Angle a it goes up the vector and down.